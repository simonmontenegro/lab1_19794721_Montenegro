#lang racket
(provide (all-defined-out))

(require "Listas.rkt")

(define esCommit?
  (lambda (commit)
    (if (null? commit)
        #t
        (if (and (string? (car (car commit))) (esString? (cdr (car commit))))
            (esCommit? (cdr commit))
            #f
            )
        )
    )
  )